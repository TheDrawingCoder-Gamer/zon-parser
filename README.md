# zon-parser

uses zig's built in AST parser to parse ZON and supports parsing it into objects like how std.json can

TODO: copy more tests from JSON

FUTURE (?): do parsing myself (?) unlikely. could pull in egg zon as a dependency.
